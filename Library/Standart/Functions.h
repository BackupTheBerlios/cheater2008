//---------------------------------------------------------------------------

#ifndef FunctionsH
#define FunctionsH
//---------------------------------------------------------------------------
int WritePointers(TStream* stream,TList* list);//returm number of pointer was writed
int ReadPointers(TStream* stream,TList* list);//return number of pointers was read
#endif
